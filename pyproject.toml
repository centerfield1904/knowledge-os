[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "knowledge-os"
version = "0.1.0"
description = "Multi-source digest pipeline with semantic topic matching and engagement detection."
requires-python = ">=3.9"

[tool.setuptools.packages.find]
where = ["src"]
